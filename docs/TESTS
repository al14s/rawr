./rawr.py --check-install
    checks version of NMap & phantomJS, will update pJS if needed - will prompt

./rawr.py --force-install
    checks version of NMap & phantomJS, will update pJS if needed - no prompt

./rawr.py -u
    updates defpass and iptocountry csv - will prompt if update is available

./rawr.py -U
    updates defpass and iptocountry csv - no prompt

./rawr.py -n scanme.nmap.org -a -p all -z --title "War and Peace" --logo "image.png" --sslv
	verify full contents of compressed folder
	check for presence of extended SSL data in .csv
	check for logo pic & "War and Peace" in the HTML report title (printable view)
	check html report for content, functionality
	check threat matrix for 'all open ports'

./rawr.py -n <sharedhost> -b -d scans -p 80 -o -x -r --downgrade
	watch for duplicate bing entries, infinite loops, errors on bing calls
	results should be located in 'scans' folder
	check ./ssl_certs, ./robots, ./crossdomain and ./cookies for content
	check server info .csv for HTTP_OPTIONS data
	watch w/ wireshark - check requests for http/1.0
	ports other than 80

./rawr.py -n <site or IP> --spider -p fuzzdb
	make sure the fuzzdb list was used
	check <log folder>/maps/  for .png maps and .txt lists of docs & links for each site

./rawr.py -n <site or IP> --spider --spider-opts d:2,l:100,t:10,s:f --alt-domains <domain>
	watch for custom spider option settings
	watch for spidering of specified external domains

./rawr.py -n <site or IP> --json
    stdout - only JSON, files created in background

./rawr.py -n <site or IP> --json-min -b -d scans -p 80 -o -x -r --downgrade --sslv -z -a
    nothing but JSON strings